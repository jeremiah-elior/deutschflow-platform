{
  "name": "deutschflow-platform",
  "private": true,
  "version": "0.0.87",
  "description": "DeutschFlow V87 production backend with fast relation-scoped mobile APIs, MySQL, and Google Read & Speak.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n API,ADMIN -c blue,magenta \"npm run dev -w @deutschflow/api\" \"npm run dev -w @deutschflow/admin\"",
    "build": "npm run build -w @deutschflow/shared && npm run build -w @deutschflow/api && npm run build -w @deutschflow/admin && node scripts/copy-admin-dist.mjs",
    "typecheck": "npm run typecheck -w @deutschflow/shared && npm run typecheck -w @deutschflow/api && npm run typecheck -w @deutschflow/admin",
    "lint": "npm run lint -w @deutschflow/api && npm run lint -w @deutschflow/admin",
    "build:admin": "npm run build -w @deutschflow/shared && npm run build -w @deutschflow/admin && node scripts/copy-admin-dist.mjs",
    "build:api": "npm run build -w @deutschflow/shared && npm run build -w @deutschflow/api",
    "build:single-domain": "npm run build",
    "start:single-domain": "node server.js",
    "start": "node server.js",
    "hostinger:build": "npm run build",
    "hostinger:start": "node server.js",
    "postinstall": "npm run build",
    "db:migrate": "node scripts/mysql-migrate.mjs",
    "storage:import": "node scripts/import-storage-backup.mjs",
    "admin:set-password": "node scripts/set-admin-password.mjs",
    "admin:create": "node scripts/create-admin.mjs",
    "build:check": "node scripts/ensure-build.mjs"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "main": "server.js",
  "dependencies": {
    "mysql2": "^3.14.3",
    "dotenv": "^16.4.5",
    "bcryptjs": "^3.0.2",
    "concurrently": "^8.2.2",
    "typescript": "^5.5.4",
    "google-auth-library": "^9.15.1"
  }
}
